<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>
<div class="container">
    <div class="row">
        <% @products.each do |product| %>
            <div class="col-3">
            <%= product.title %>
            <%= product.description %>
            <%= product.brand.name %>
            <%= product.category.name %>
            </div>
        <% end %>
    </div>
</div>